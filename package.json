{
  "name": "astro-search-params",
  "type": "module",
  "description": "Search param hooks for Astro",
  "main": "dist/index.js",
  "scripts": {
    "build": "pnpm run -r build",
    "test": "vitest run",
    "clean": "rimraf **/dist",
    "publish": "pnpm run clean && pnpm run build && pnpm publish -r"
  },
  "keywords": [
    "astro",
    "search params"
  ],
  "author": "Felix Eklöf",
  "license": "MIT",
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "package.json"
  ],
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}